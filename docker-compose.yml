services:
  api:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.api
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      APP_NAME: OpsPilot AI
      APP_ENV: local
      APP_DEBUG: "true"
      LOG_LEVEL: INFO
